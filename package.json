{
  "name": "express",
  "version": "1.0.0",
  "description": "mongoose + express + morgan",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "node ./api/seed",
    "dev": "nodemon",
    "start": "node ./index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.17.1",
    "mongoose": "^5.10.5",
    "morgan": "^1.10.0"
  }
}
